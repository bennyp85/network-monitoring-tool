import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;public class NetworkDeviceTest {
    @Test
    public void testCheckStatus() {
        NetworkDevice device = new NetworkDevice();
        device.CheckStatus();
        // Add assertions here to verify the CheckStatus method works as expected
    }
}